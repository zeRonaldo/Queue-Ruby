<%= form_with(model: enqueue, local: true) do |form| %>
  <% if enqueue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(enqueue.errors.count, "error") %> prohibited this enqueue from being saved:</h2>

      <ul>
      <% enqueue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :descrição %>
    <%= form.text_field :description %>
  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
